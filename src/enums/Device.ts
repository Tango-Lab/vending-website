// Define enums for ConnectivityStatus and PowerStatus if not already defined
export enum ConnectivityStatus {
  ONLINE = 'ONLINE',
  OFFLINE = 'OFFLINE',
  MAINTENANCE = 'MAINTENANCE',
  UNKNOWN = 'UNKNOWN',
}

export enum PowerStatus {
  ON = 'ON',
  OFF = 'OFF',
  UNKNOWN = 'UNKNOWN',
}