'use client';
import React, { useState } from 'react';

const MobileSideBar = () => {
  const [isCollapse, setIsCollapse] = useState(false);

  return <div className="sm:container mx-auto max-md:block hidden"></div>;
};

export default MobileSideBar;
